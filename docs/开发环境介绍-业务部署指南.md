# 开发环境（Dev）介绍 — 业务部署指南

> **文档性质**：面向业务开发同事的标准环境说明，说明 Dev 能用什么、如何接入、如何部署服务。  
> **更新日期**：2026-06-17  
> **维护**：infra / 运维团队（变更后同步本文档与 [资产台账](../assets/registry.yaml)）

---

## 一、快速了解

| 项 | 说明 |
|----|------|
| **环境名称** | Dev（开发环境） |
| **逻辑标识** | `env_name: dev` / 应用内建议 `ENV=development` |
| **地域** | 阿里云华东 1（杭州） |
| **当前状态** | 主机基线、WireGuard、Dev 数据库、Hub 管理面、**Compose LE 网关 + PetIntelli** 已就绪；五人 WG Client 池 Hub 已登记（四人待接入）；**GitHub Runner 自动发布待接通** |
| **业务代码放哪** | 独立 **app-repo**（业务仓库）；本仓库 **infra-ops** 只管机器与基础设施 |

**一句话**：Dev 是一台（现阶段）已装好 Docker、已连上独立 MySQL 库 `app_dev` 的云服务器；你负责在 **app-repo** 里构建镜像与业务配置，由 CI/CD 或运维协助发布到 Dev。

---

## 二、架构总览

```text
开发人员
    │
    ├── WireGuard Client（10.200.10.x）──→ 管理 / Dev / Test 内网
    │       └── 可选：JumpServer https://jms.internal（经 Hub 反代）
    │
    └── app-repo：push develop ──→ （规划）构建镜像 ──→ 部署到 Dev

┌─────────────────────────────────────────────────────────────┐
│  dev-01 / ci-01（同机 yax，121.41.58.20）                    │
│    私网 172.21.226.38  │  WG 10.200.0.2                      │
│    · Docker / PetIntelli @ 127.0.0.1:8080（✅ operational）     │
│    · Compose LE 网关 80/443 → 8080（✅ operational）            │
│    · Ansible 控制机 / 未来 GitHub Runner                     │
├─────────────────────────────────────────────────────────────┤
│  dev-02（112.124.104.223）— Worker / 队列（Bootstrap 待完成）   │
├─────────────────────────────────────────────────────────────┤
│  hub-01（121.43.49.58）— WireGuard Server 10.200.0.1        │
│    · Nginx 网关 + dnsmasq（*.internal）                       │
│    · JumpServer 堡垒机 @ https://jms.internal（已上线）      │
└─────────────────────────────────────────────────────────────┘
         │ VPC 内网                    │ RAM 角色（Dev-ECS-Role）
         ▼                             ▼
   RDS MySQL app_dev              OSS infra-dev-file-storage
   （已验收）                      （2026-06-22 已验收）
         │
         └── 云 Redis r-bp10kgwdml4kqgssoz（app: DB 0；veterinaryAgent: DB 2）
```

---

## 三、仓库分工（必读）

| 仓库 | 职责 | 谁改 |
|------|------|------|
| **app-repo** | 业务代码、Dockerfile、单元测试、数据库迁移脚本、应用 CI（构建镜像） | 业务开发 |
| **infra-ops** | ECS 基线、Inventory、Ansible Playbook、基础设施 CI、部署编排 | infra / 运维 |

**不要**在 app-repo 里改服务器 SSH、安全组、WireGuard；**不要**在 infra-ops 里写业务逻辑。

### 目标发布流程（规划态）

```text
1. 开发在 app-repo 提交到 develop 分支
2. app-repo CI 构建 Docker 镜像并推送到镜像仓库（ACR 或私有 Registry）
3. 触发 infra-ops 的 deploy（或 app-repo 直接调部署 API）
4. Ansible 更新 Dev 上的镜像标签与环境变量
5. docker compose pull && up -d
6. 健康检查 /health 通过
```

> **当前（2026-06-17）**：`gateway-compose` / Compose LE 网关 **operational**；PetIntelli @ 8080；**GitHub Runner 自动触发** 待注册。验收见 [阶段 4](../acceptance/20260617-阶段4-Dev-Gateway-Compose-LE验收.md)。

---

## 四、计算资源（ECS）

### 4.1 当前可用节点

| 主机 | 角色 | 公网 IP | VPC 私网 IP | WG 地址 | 状态 |
|------|------|---------|-------------|---------|------|
| **dev-01** | 主应用 Web/API | 121.41.58.20 | 172.21.226.38 | （经 ci-01）10.200.0.2 | **PetIntelli + Compose LE 网关** ✅ |
| **ci-01** | CI / Ansible 控制机 | 同上（与 dev-01 同机） | 同上 | 10.200.0.2 | WG 已通 |
| **dev-02** | Worker / 队列 | 112.124.104.223 | 172.21.127.124 | 10.200.1.2（规划） | **Bootstrap 未完成** |
| **hub-01** | WG Server / 堡垒机 / 内网 DNS | 121.43.49.58 | 172.21.127.123 | 10.200.0.1 | 管理面 operational；**不跑业务** |

### 4.2 应用部署落点（现阶段）

| 组件 | 部署位置 | 说明 |
|------|----------|------|
| Web / API 主服务 | **dev-01** | PetIntelli @ `127.0.0.1:8080`；`https://backend.yizuxing.com` |
| MQTT（公网） | **dev-01** 网关 | `mqtts://mqtt.yizuxing.com:8883` → 本机 `127.0.0.1:1883` |
| Redis 缓存 | **云实例**（非 ECS） | `r-bp10kgwdml4kqgssoz`；见第七节 |
| 队列 Worker | **dev-02**（规划） | Bootstrap 后部署；Redis 已不依赖本机 |
| 数据库 | **RDS**（非 ECS） | 见第五节 |

### 4.3 系统与目录约定

| 项 | 值 |
|----|-----|
| 操作系统 | Debian 12 |
| 部署用户 | `deploy`（SSH 密钥登录，已禁用 root 日常登录） |
| 应用目录 | `/opt/app`、`/opt/app/compose` |
| 应用日志 | `/var/log/app` |
| 时区 | `Asia/Shanghai` |

---

## 五、数据库（MySQL / RDS）

Dev 使用阿里云 **RDS MySQL 8.0** 上的**独立库**，与生产库隔离（方案 A，已验收）。

### 5.1 连接参数（应用配置用）

| 变量名（建议） | 值 | 备注 |
|----------------|-----|------|
| `DB_HOST` | `rm-bp1wjjf373l7t331v.mysql.rds.aliyuncs.com` | **必须用内网域名** |
| `DB_PORT` | `3306` | |
| `DB_NAME` | `app_dev` | |
| `DB_USER` | `app_dev` | 仅有权访问 `app_dev` 库 |
| `DB_PASSWORD` | 向 infra 申请 | 不进 Git；CI 用 GitHub Secret `DB_PASSWORD` |

**JDBC 示例**（Java）：

```text
jdbc:mysql://rm-bp1wjjf373l7t331v.mysql.rds.aliyuncs.com:3306/app_dev?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
```

**注意**：

- 不要使用带 `no` 后缀的**外网**域名（`...vno...`）。
- 从**办公网笔记本**无法直连 RDS（除非你在 VPC 内）；应用在 **ECS 上运行**时自动走 VPC 内网。
- `app_dev` 账号**不能**访问 `yzx`、`risk` 等生产库（已验收隔离）。

### 5.2 Schema 迁移

- 迁移脚本放在 **app-repo**（Flyway / Liquibase / 框架自带迁移均可）。
- Dev 环境允许自动执行迁移；**禁止**连接或导入生产数据。
- 字符集请使用 **utf8mb4**。

详情：[RDS 实例现状与 Dev 规划](../rds/20260615-RDS-MySQL-实例现状与Dev规划.md) · [阶段 G 验收报告](../acceptance/20260615-阶段G-Dev-RDS-app_dev验收.md)

---

## 六、对象存储（OSS）

| 变量名（建议） | 值 |
|----------------|-----|
| `OSS_BUCKET` | `infra-dev-file-storage`（Dev 专用，与生产 `yzx-file-storage` 隔离） |
| `OSS_PREFIX` | 空（桶级隔离；应用对象键如 `uploads/...`，**不再使用** `dev/` 前缀） |
| `OSS_ENDPOINT` | `oss-cn-hangzhou-internal.aliyuncs.com`（ECS 内网） |

- 访问方式：ECS 绑定 RAM 角色 **Dev-ECS-Role** + 策略 **DevOssInfraDevFileStorageOnly**；**不要在代码或 `.env` 里写 AK/SK**。
- 验收：`make oss-smoke`（2026-06-22 通过）。
- **权威台账**：[OSS 实例现状与 Dev 规划](../oss/20260616-OSS-实例现状与Dev规划.md) · [阶段 H 验收报告](../acceptance/20260622-阶段H-Dev-OSS验收.md)。

---

## 七、缓存与消息队列（Redis）

| 组件 | 现状 | 连接方式 |
|------|------|----------|
| **Redis** | **已就绪**（2026-06-22 阶段 I 验收） | 见下表 |
| 消息队列 | **未单独部署** | 可与 Redis 共用或后续选型 |

**云 Redis（托管实例，非 dev-02 Docker）**

| 项目 | `REDIS_USERNAME` | `REDIS_DB` | `REDIS_KEY_PREFIX` | 密码来源 |
|------|------------------|------------|--------------------|----------|
| `petintelli-backend` | `petintelli_app` | `0` | `petintelli:app:` | GitHub Secret `REDIS_PASSWORD` |
| `veterinaryAgent` | `petintelli_agent` | `2` | `veterinaryAgent:` | GitHub Secret `REDIS_AGENT_PASSWORD` |

公共参数：

| 变量 | Dev 值 |
|------|--------|
| `REDIS_HOST` | `r-bp10kgwdml4kqgssoz.redis.rds.aliyuncs.com`（**内网**） |
| `REDIS_PORT` | `6379` |

验收示例（在 dev-01 / ci-01 同机）：

```bash
export REDIS_HOST="r-bp10kgwdml4kqgssoz.redis.rds.aliyuncs.com"
export REDIS_PORT="6379"
nc -zv "$REDIS_HOST" "$REDIS_PORT"
redis-cli -h "$REDIS_HOST" -p "$REDIS_PORT" \
  --user petintelli_app -a "$REDIS_PASSWORD" --no-auth-warning PING
```

- `veterinaryAgent` 使用同样的 `REDIS_*` 键名，但值切换为 `petintelli_agent` / DB `2` / `veterinaryAgent:`；密码向 infra 申请 `REDIS_AGENT_PASSWORD`。
- **权威台账**：[Redis 实例现状与 Dev 规划](../redis/20260622-Redis-实例现状与Dev规划.md) · [阶段 I 验收报告](../acceptance/20260622-阶段I-Dev-Redis验收.md)  
- **说明**：原规划在 dev-02 自建 Redis；已改为云实例，**不依赖 dev-02 Bootstrap** 即可使用缓存。

---

## 八、网络接入（开发人员）

### 8.1 推荐：WireGuard

用于 SSH 调试、访问内网 HTTP API、访问 `10.200.0.2`（CI/Dev 同机）、解析 `*.internal` 域名等。

| 项 | 值 |
|----|-----|
| Hub 地址 | `121.43.49.58:51820`（UDP） |
| Hub WG IP | `10.200.0.1` |
| CI / Dev 同机 WG IP | `10.200.0.2` |
| 笔记本地址段 | `10.200.10.0/24`（每人独立 Peer，向 infra 申请） |
| 内网 DNS | `10.200.0.1`（WG 配置中 `DNS = 10.200.0.1`） |
| 开发角色可路由网段 | `10.200.0.0/24`（管理）、`10.200.1.0/24`（Dev）、`10.200.2.0/24`（Test） |
| 生产网段 | `10.200.3.0/24` **不在**开发笔记本路由内 |

**人员 Client 池**（2026-06-16）：台账见 [wireguard-clients.yaml](../assets/wireguard-clients.yaml)。Hub 已为五人登记 Peer；接入前请向 infra 领取个人 `wg0.conf`（勿共用密钥）。

**接入步骤**：[办公笔记本 WireGuard Client 接入指南](../wireguard/办公笔记本-WG-Client-接入指南.md)

**若同时使用 Clash Verge（跨境代理）**：须配置 bypass，避免影响 WG 与 SSH → [Clash Verge 与 WG/SSH 共存指南](../wireguard/clash-verge-wg-ssh-bypass.md)（Windows / macOS / Linux）。

验收示例（连接 WG 后）：

```bash
ping -c 2 10.200.0.1          # Hub
ping -c 2 10.200.0.2          # CI / Dev
ping -c 1 jms.internal        # 内网 DNS → 10.200.0.1
curl -sk -o /dev/null -w '%{http_code}\n' https://jms.internal/   # 期望 200
curl -sk -o /dev/null -w '%{http_code}\n' https://dev-app.internal/health   # 期望 200
ssh deploy@10.200.0.2         # 需 infra 发放的 SSH 私钥（如 infra-ci-deploy）
```

### 8.2 JumpServer 堡垒机（可选，授权后使用）

| 项 | 值 |
|----|-----|
| 访问地址 | `https://jms.internal/`（须先连接 WireGuard） |
| 前提 | WG 隧道已建立；infra 已开通平台账号与资产授权 |
| 用途 | Web 终端 / SFTP 登录已纳管主机（Hub-01、Dev-01 / CI-DEV-01） |
| 注意 | `deploy` 用户供 CI/Ansible 使用，**不在** JumpServer 中；日常运维登录走 `jump_ops` 等已授权账户 |

平台账号、MFA 仍在推进中；Hub-01 与 Dev-01（`CI-DEV-01` @ `10.200.0.2`）已完成 Ansible 纳管与 Web 终端验收（2026-06-18）。未获授权前请通过 WG + SSH 按 infra 指引操作。

### 8.3 应用 HTTP 访问

| 方式 | 地址 | 说明 |
|------|------|------|
| **WG 内网（推荐）** | `http://dev-app.internal/healthz` | 须连 WG；内网 HTTP（Compose） |
| 设备管理系统（WG） | `http://dev-app.internal/device-management/` | 前端静态文件；API 走 `/device-management/api/*` |
| WG 直连 IP | `http://10.200.0.2/healthz` | 同机 WG 地址 |
| **公网 / 小程序** | `https://backend.yizuxing.com/healthz` | LE 证书；须配微信合法域名 |
| 设备管理系统（公网） | `https://backend.yizuxing.com/device-management/` | 路径隔离；后端 `127.0.0.1:18080` |
| 公网 IP（过渡） | `https://121.41.58.20/healthz` | 证书 SAN 以域名为准 |
| 本机（ECS 上） | `http://127.0.0.1:8080/healthz` | 不经 Nginx，仅调试 |
| 设备管理系统本机后端 | `http://127.0.0.1:18080/healthz` | 不经 Nginx，仅调试 |
| HTTP 跳转 | `http://backend.yizuxing.com/...` | 301 → HTTPS |
| **MQTT（设备）** | `mqtts://mqtt.yizuxing.com:8883` | Nginx stream TLS；Broker 仅 `127.0.0.1:1883` |

**注意**：8080、18080 **不对公网开放**；外网/WG 均走 Compose Nginx。`/device-management/api/*` 会剥离 `/device-management` 前缀后转发到后端原生 `/api/*`。网关见 [Dev Gateway Runbook](../docker/dev-gateway.runbook.md)。

---

## 九、环境变量规范

应用配置（`.env`、K8s ConfigMap、Spring `application-dev.yml` 等）建议与 infra 约定**键名统一**，仅值因环境而异：

| 类别 | 键名示例 | Dev 值要点 |
|------|----------|------------|
| 环境标识 | `ENV` | `development` |
| 数据库 | `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD` | 见第五节 |
| OSS | `OSS_BUCKET`, `OSS_PREFIX` | 见第六节 |
| Redis | `REDIS_HOST`, `REDIS_PORT`, `REDIS_USERNAME`, `REDIS_PASSWORD`, `REDIS_DB` | 见第七节；按项目选择账号、DB 和 `REDIS_KEY_PREFIX` |
| 日志 | `LOG_LEVEL` | Dev 可用 `DEBUG` |
| 功能开关 | `SWAGGER_ENABLED` | Dev 建议 `true` |

**禁止**：

- 将含密码的 `.env` 提交到 Git  
- 在 Dev 配置中指向生产库、生产 OSS 前缀（`prod/`）  
- 硬编码 `47.98.161.33`、`47.97.19.58` 等**已退役** IP

---

## 十、如何部署业务服务

### 10.1 部署前检查清单

- [ ] 已拿到 `app_dev` 数据库密码（或 CI Secret 已配置）
- [ ] Dockerfile 可在本地构建通过
- [ ] 健康检查路径约定（建议 `/health` 返回 200）
- [ ] 监听端口与 infra 约定一致（默认应用 **8080**，Nginx 反代 80）
- [ ] 若依赖 Redis：已配置 `REDIS_*`（✅ 云实例 2026-06-22；密码向 infra 申请）
- [ ] 若依赖 OSS：已确认 `OSS_BUCKET=infra-dev-file-storage` 且 RAM 已绑定（✅ 2026-06-22）
- [ ] 已与 infra 确认当前发布方式（手工 vs 流水线）

### 10.2 方式 A：等待标准流水线（推荐目标态）

1. app-repo 配置 CI：构建镜像并打标签 `app:dev-<git-sha>`  
2. 镜像推送到团队 Registry（ACR 等）  
3. 触发 infra-ops `deploy.yml`（GitHub Actions，environment=`dev`）  
4. infra 执行 `dev-app.yml` 更新 Compose 与 `.env`（**Playbook 已就绪**）

> GitHub Self-hosted Runner **尚未注册**；接通前请由 infra 在 ci-01 手工执行 playbook，或使用方式 B。

### 10.3 方式 B：开发期临时部署（需 infra 授权）

在 **已接入 WireGuard** 的前提下，由有权限同事在 **ci-01** 上：

1. SSH：`ssh deploy@10.200.0.2`（或私网 `172.21.226.38`）  
2. 将镜像拉到 dev-01，或 `docker compose` 于 `/opt/app/compose` 启动  
3. 环境变量写入 `/opt/app/compose/.env`（**勿提交 Git**）  

**禁止**：

- `scp` 二进制手工上生产（Prod 将来同样禁止）  
- 在未授权情况下直接改 Nginx / 系统服务  
- 在 Hub 上部署业务容器  

### 10.4 数据库初始化

1. 在 app-repo 提供迁移脚本  
2. 首次部署前在 Dev 执行迁移（本地连库需通过 ECS 跳板或请 infra 代执行）  
3. Dev 库可重建；**勿**把迁移直接对生产库执行  

---

## 十一、运维边界与「三条铁律」

| 规则 | 对开发的含义 |
|------|----------------|
| **环境隔离** | 只用 `app_dev`、OSS `infra-dev-file-storage`；不要连 prod 库 / `yzx-file-storage` |
| **发布走 CI/CD** | 不要长期依赖手工改服务器上的 jar/容器 |
| **运维走内网通道** | SSH 经 WG 或 JumpServer（`jms.internal`）；不要要求开放 `0.0.0.0/0` SSH |

问题反馈、开白名单、申请 WG Peer / DB 密码：联系 **infra / 运维**（勿在群里发密码明文）。

---

## 十二、当前限制与路线图

| 能力 | 状态 | 说明 |
|------|------|------|
| dev-01 主机 + Docker | ✅ 就绪 | |
| Dev Compose LE 网关 + PetIntelli | ✅ 就绪 | 2026-06-17 阶段 4 验收 |
| Dev 占位 API + 宿主机 Nginx（host） | ✅ 历史 | 2026-06-16 阶段 3；公网 HTTPS 已由阶段 4 取代 |
| Dev 数据库 `app_dev` | ✅ 就绪 | 2026-06-15 验收 |
| WireGuard Hub + ci-01 | ✅ 就绪 | 2026-06-14 验收 |
| WG 人员 Client 池（5 人） | ⏳ 部分就绪 | Hub 已登记；zhengyaoyuan operational；四人待 `wg0.conf` 与握手 |
| Hub 内网 DNS（`*.internal`） | ✅ 就绪 | 2026-06-16 G2 |
| JumpServer（`jms.internal`） | ✅ 已上线 | 2026-06-17 G4；Hub + Dev-01 纳管 ✅（2026-06-18）；平台授权 / MFA ⏳ |
| Dev Redis 云实例 | ✅ 就绪 | `petintelli_app` / DB 0；`petintelli_agent` / DB 2 for `veterinaryAgent`（pending smoke） |
| dev-02 Bootstrap | ⏳ 待完成 | Worker/队列；Redis 已不依赖本机 |
| OSS RAM + Dev 桶 | ✅ 就绪 | 2026-06-22 `make oss-smoke` |
| GitHub Self-hosted Runner | ⏳ 未注册 | |
| `dev-app.yml` 自动部署（CI 触发） | ⏳ 待 Runner | Playbook 已实现；占位 API 已验收 |
| 真实业务镜像 | ⏳ 待 app-repo | 替换 `deploy_status: placeholder` |
| 公网 Dev 域名 + 受信 HTTPS | ⏳ 未配置 | 当前自签 + EIP |
| Test / Prod | ❌ 本期不做 | |

---

## 十三、相关文档索引

| 文档 | 适用场景 |
|------|----------|
| [企业开发环境（Dev）实施方案](../20260608-ECS%20企业开发环境（Dev）实施方案.md) | 总体方案与阶段划分 |
| [开发环境部署计划](../plan/20260608-开发环境（Dev）部署计划.md) | infra 实施进度 |
| [资产总台账](../assets/registry.yaml) | IP、角色、替代关系 |
| [RDS Dev 规划](../rds/20260615-RDS-MySQL-实例现状与Dev规划.md) | 数据库细节 |
| [OSS Dev 规划](../oss/20260616-OSS-实例现状与Dev规划.md) | 对象存储现状与 RAM/桶级隔离 |
| [阶段 H OSS 验收](../acceptance/20260622-阶段H-Dev-OSS验收.md) | `make oss-smoke` 验收报告 |
| [Redis Dev 规划](../redis/20260622-Redis-实例现状与Dev规划.md) | 云 Redis 连接与白名单 |
| [阶段 I Redis 验收](../acceptance/20260622-阶段I-Dev-Redis验收.md) | 缓存层验收 |
| [WireGuard 笔记本接入](../wireguard/办公笔记本-WG-Client-接入指南.md) | 开发联调网络 |
| [WG 人员 Client 台账](../assets/wireguard-clients.yaml) | Peer 分配与接入状态 |
| [阶段 F WG 验收](../acceptance/20260614-阶段F-WireGuard验收报告.md) | Hub↔ci-01 隧道验收 |
| [阶段 F6 Client 池验收](../acceptance/20260616-阶段F6-WireGuard人员Client池验收.md) | 五人笔记本 Hub 登记验收 |
| [阶段 G RDS 验收](../acceptance/20260615-阶段G-Dev-RDS-app_dev验收.md) | 数据库验收 |
| [阶段 G5 JumpServer Hub 纳管](../acceptance/20260617-阶段G5-JumpServer资产纳管-Hub验收.md) | Hub-01 堡垒机资产 |
| [阶段 G5 JumpServer Dev 纳管](../acceptance/20260618-阶段G5-JumpServer资产纳管-Dev验收.md) | Dev-01（CI-DEV-01）堡垒机资产 |
| [阶段 4 Dev Gateway Compose 验收](../acceptance/20260617-阶段4-Dev-Gateway-Compose-LE验收.md) | LE 网关 + 微信 TLS |
| [Dev Gateway device-management 路径隔离验收](../acceptance/20260630-Dev-Gateway-device-management路径隔离验收.md) | `/device-management` 前端与 API 入口 |
| [阶段 3 Dev 业务 Nginx 验收](../acceptance/20260616-阶段3-Dev业务Nginx与占位API验收.md) | 宿主机 Nginx（历史） |
| [Dev Gateway Runbook](../docker/dev-gateway.runbook.md) | Compose 网关运维 |
| [contributing.md](../contributing.md) | 修改 infra-ops 仓库时 |

---

## 修订历史

| 日期 | 说明 |
|------|------|
| 2026-06-15 | 初版：汇总阶段 F（WG）、阶段 G（RDS app_dev）完成后的开发接入说明 |
| 2026-06-16 | 同步 F6 五人 WG Client 池、Hub 内网 DNS / JumpServer 上线状态与接入验收示例 |
| 2026-06-30 | device-management-system 通过 `/device-management` 路径隔离接入业务面 Compose Nginx；网关镜像 1.0.3 |
| 2026-06-22 | 阶段 J：API 域 `backend.yizuxing.com`；MQTT 路线 A `mqtt.yizuxing.com:8883`；网关镜像 1.0.2 |
| 2026-06-17 | 阶段 4：Compose LE 网关 operational；Docker 网段修复（历史域名 jxqydw） |
| 2026-06-16 | 阶段 3：宿主机 Nginx + 占位 API operational |
