# =============================================================================
# 文件: docker/mem0/template/mem0.dev.env.template
# 作用: 提供开发环境 Mem0 REST Server 敏感参数模板。
# 范围: 仅保存 LiteLLM 访问密钥、Mem0 管理 key、JWT secret 与数据库密码。
# 说明: 共享开发环境应复制为 docker/mem0/template/mem0.dev.env 后替换密钥。
# =============================================================================

# Mem0 通过 OpenAI 兼容协议访问 LiteLLM；此值应等于 LiteLLM master key。
OPENAI_API_KEY=sk-dev-litellm-change-me

# Mem0 legacy 管理 API key；Agent 侧 MEM0_API_KEY 应与此值一致。
ADMIN_API_KEY=sk-dev-mem0-change-me

# Mem0 Dashboard/JWT 鉴权密钥；即使开发环境也应保持足够长度。
JWT_SECRET=dev-mem0-jwt-secret-change-me-at-least-32-chars

# Mem0 访问 PostgreSQL 逻辑库的密码；用户名和库名位于 application.yml。
POSTGRES_PASSWORD=mem0
