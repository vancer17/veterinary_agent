# =============================================================================
# 文件: docker/mem0-dashboard/template/mem0-dashboard.prod.env.template
# 作用: 提供正式环境 Mem0 运维 Dashboard 敏感参数模板。
# 范围: 当前 Dashboard 无必填敏感参数，保留文件用于统一 env 挂载结构。
# 说明: 正式环境如后续引入 Dashboard 专属密钥，应写入 mem0-dashboard.prod.env，不进入版本控制。
# =============================================================================

# 当前无必填敏感参数。
